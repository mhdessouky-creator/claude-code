# 📱 دليل تشغيل الوكيل الذكي على الموبايل

## 🎯 الطرق المتاحة للتشغيل

### ✅ الطريقة 1: Termux (للتشغيل المباشر على الموبايل)

#### الخطوات:

1. **تثبيت Termux:**
   - حمل Termux من F-Droid: https://f-droid.org/en/packages/com.termux/
   - أو من Google Play (إصدار قديم)

2. **إعداد البيئة:**
```bash
# داخل Termux
pkg update && pkg upgrade
pkg install nodejs git
```

3. **استنساخ المشروع:**
```bash
# إذا كان المشروع على GitHub
git clone https://github.com/mhdessouky-creator/claude-code.git
cd claude-code

# أو إذا كان عندك محلياً
cd /sdcard/Download/claude-code
```

4. **التثبيت والتشغيل:**
```bash
npm install
./start-agent.sh
```

---

### ✅ الطريقة 2: Replit (الأسهل - عبر المتصفح)

#### الخطوات:

1. افتح **Replit.com** من المتصفح
2. ارفع المشروع أو استورده من GitHub
3. عدل ملف `.env` وأضف API key
4. اضغط **Run** ▶️
5. استخدم الوكيل من المتصفح مباشرة!

**ميزة:** لا يحتاج تثبيت أي شيء على الموبايل

---

### ✅ الطريقة 3: GitHub Codespaces

#### الخطوات:

1. افتح المستودع على GitHub من المتصفح
2. اضغط على **Code** > **Codespaces** > **Create codespace**
3. انتظر تجهيز البيئة
4. عدل `.env` وأضف API key
5. شغل: `./start-agent.sh`

**ميزة:** بيئة كاملة مجانية في السحابة!

---

## 🔑 الحصول على Anthropic API Key

### الخطوات:

1. افتح: https://console.anthropic.com
2. سجل حساب جديد (مجاني)
3. اذهب إلى **API Keys**
4. اضغط **Create Key**
5. انسخ المفتاح (يبدأ بـ `sk-ant-...`)

### إضافة المفتاح:

عدل ملف `.env`:
```bash
ANTHROPIC_API_KEY=sk-ant-api03-your-actual-key-here
```

⚠️ **مهم:** المفتاح سري! لا تشاركه مع أحد

---

## 🚀 تشغيل الوكيل

### استخدام السكريبت السريع:
```bash
./start-agent.sh
```

### أو مباشرة:

#### 1. محادثة تفاعلية:
```bash
npm run cli chat
```

#### 2. تنفيذ مهمة واحدة:
```bash
npm run cli task "نظم ملفات Downloads"
```

#### 3. عرض الحالة:
```bash
npm run cli status
```

#### 4. مثال تجريبي:
```bash
npm start
```

---

## 💡 أمثلة على المهام

### إدارة الملفات:
```bash
npm run cli task "نظم الملفات في مجلد Documents حسب النوع"
npm run cli task "ابحث عن ملفات PDF في Downloads"
npm run cli task "احذف الملفات الأقدم من 30 يوم في مجلد temp"
```

### البحث والمعلومات:
```bash
npm run cli task "ابحث عن أحدث أخبار الذكاء الاصطناعي"
npm run cli task "اعطني ملخص عن Claude AI"
```

### جدولة المهام:
```bash
# كل يوم الساعة 9 صباحاً
npm run cli schedule "تنظيف الملفات المؤقتة" "0 9 * * *"

# كل ساعة
npm run cli schedule "التحقق من التحديثات" "0 * * * *"
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: `command not found: node`
**الحل:**
```bash
# في Termux
pkg install nodejs

# التحقق من التثبيت
node --version
npm --version
```

### المشكلة: `API key is required`
**الحل:**
- تأكد أن ملف `.env` موجود
- تأكد أن API key مكتوب صح
- المفتاح يبدأ بـ `sk-ant-`

### المشكلة: `EACCES permission denied`
**الحل:**
```bash
# في Termux
chmod +x start-agent.sh
chmod -R 755 node_modules
```

### المشكلة: نفذت الذاكرة على الموبايل
**الحل:**
- استخدم Replit أو Codespaces بدلاً من Termux
- قلل `MAX_TOKENS` في `.env`

---

## 📊 متطلبات النظام

### للتشغيل على Termux:
- ✅ موبايل أندرويد 7.0+
- ✅ 2GB RAM على الأقل
- ✅ 500MB مساحة فارغة

### للتشغيل على Replit/Codespaces:
- ✅ متصفح حديث فقط
- ✅ اتصال إنترنت مستقر

---

## 🎓 نصائح للاستخدام على الموبايل

1. **استخدم لوحة مفاتيح خارجية** للكتابة الأسرع
2. **Termux Widget** لتشغيل سكريبتات مباشرة
3. **SSH من الكمبيوتر** إلى Termux للتحكم عن بعد
4. **Replit Mobile App** للتطوير المريح

---

## 🔗 روابط مفيدة

- 📖 الدليل الشامل: `AGENT_GUIDE.md`
- 🔧 الإعدادات المتقدمة: `.env.example`
- 🐙 GitHub: https://github.com/mhdessouky-creator/claude-code
- 🤖 Anthropic Console: https://console.anthropic.com
- 📱 Termux: https://f-droid.org/en/packages/com.termux/

---

## 💬 الدعم

إذا واجهت مشكلة:
1. راجع قسم **حل المشاكل** أعلاه
2. افتح **Issue** على GitHub
3. تأكد من إصدار Node.js: `node --version` (يجب 18+)

---

**صنع بـ ❤️ للمستخدمين العرب**
