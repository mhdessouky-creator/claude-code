# 🚀 دليل تشغيل الوكيل على Replit - خطوة بخطوة

## 📱 التشغيل على Replit (من الموبايل أو الكمبيوتر)

### الخطوة 1️⃣: إنشاء حساب على Replit

1. افتح المتصفح (Chrome أو Firefox)
2. اذهب إلى: **https://replit.com**
3. اضغط على **Sign Up** في أعلى اليمين
4. سجل باستخدام:
   - Google Account (الأسهل) ✅
   - أو GitHub Account
   - أو Email + Password

---

### الخطوة 2️⃣: استيراد المشروع من GitHub

#### الطريقة الأولى - من URL مباشرة:

1. بعد تسجيل الدخول، اضغط على **+ Create Repl**
2. في الجانب الأيمن، اختر **Import from GitHub**
3. الصق رابط المستودع:
   ```
   https://github.com/mhdessouky-creator/claude-code
   ```
4. اضغط **Import from GitHub**
5. انتظر تحميل المشروع (قد يستغرق دقيقة)

#### الطريقة الثانية - من Template:

1. اضغط **+ Create Repl**
2. اختر **Node.js** كنوع المشروع
3. سمي المشروع: `digital-life-agent`
4. اضغط **Create Repl**
5. بعد فتح المحرر:
   - اضغط **Shell** في الأسفل
   - اكتب:
   ```bash
   git clone https://github.com/mhdessouky-creator/claude-code.git .
   npm install
   ```

---

### الخطوة 3️⃣: الحصول على Anthropic API Key

**هذي الخطوة الأهم! 🔑**

1. افتح تاب جديد في المتصفح
2. اذهب إلى: **https://console.anthropic.com**
3. سجل حساب جديد:
   - ادخل Email
   - تأكد من الإيميل
   - سجل دخول
4. من القائمة الجانبية، اختر **API Keys**
5. اضغط على **Create Key**
6. سمي المفتاح: `my-agent-key`
7. اضغط **Create Key**
8. **انسخ المفتاح فوراً!** (يبدأ بـ `sk-ant-api03-...`)
   - ⚠️ المفتاح يظهر مرة واحدة فقط!
   - احفظه في مكان آمن

---

### الخطوة 4️⃣: إضافة API Key في Replit

#### الطريقة الآمنة - Secrets (موصى بها):

1. في Replit، ابحث عن أيقونة **🔒 Secrets** في الشريط الجانبي
2. اضغط عليها
3. اضغط **+ New Secret**
4. في **Key**، اكتب:
   ```
   ANTHROPIC_API_KEY
   ```
5. في **Value**، الصق المفتاح اللي نسخته:
   ```
   sk-ant-api03-your-actual-key-here-xxxxxxxxxxxxxx
   ```
6. اضغط **Add Secret**

#### الطريقة البديلة - ملف .env:

1. في Replit، افتح ملف `.env` من قائمة الملفات
2. إذا الملف مش موجود، اضغط **+ New file** واسمه `.env`
3. عدل السطر الأول:
   ```bash
   ANTHROPIC_API_KEY=sk-ant-api03-your-actual-key-here
   ```
4. احفظ الملف (Ctrl+S)

⚠️ **ملاحظة:** ملف `.env` محمي تلقائياً في Replit ومش هيظهر للناس

---

### الخطوة 5️⃣: تثبيت المكتبات المطلوبة

1. اضغط على **Shell** في الأسفل
2. اكتب الأمر التالي:
   ```bash
   npm install
   ```
3. انتظر تثبيت المكتبات (1-2 دقيقة)
4. لو ظهر أي تحذير (warning)، لا تقلق - عادي!

---

### الخطوة 6️⃣: تشغيل الوكيل! 🎉

#### اختيار 1: تشغيل سريع (مثال تجريبي):

1. اضغط زر **▶ Run** الكبير في الأعلى
2. الوكيل هيشتغل ويرسل رسالة تجريبية
3. شوف النتيجة في Console

#### اختيار 2: محادثة تفاعلية:

في Shell، اكتب:
```bash
npm run cli chat
```

الوكيل هيبدأ المحادثة:
```
🤖 AI Agent - Interactive Chat

You: مرحباً
Agent: أهلاً! كيف يمكنني مساعدتك؟

You: نظم ملفات Downloads
Agent: سأقوم بتنظيم الملفات...
```

للخروج، اكتب: `/exit`

#### اختيار 3: تنفيذ مهمة مباشرة:

```bash
npm run cli task "ابحث عن أحدث أخبار الذكاء الاصطناعي"
```

#### اختيار 4: السكريبت السهل:

```bash
./start-agent.sh
```
اختر من القائمة (1-4)

---

### الخطوة 7️⃣: تجربة الوكيل بمهام حقيقية

جرب الأوامر دي:

```bash
# تنظيم ملفات
npm run cli task "نظم الملفات حسب النوع"

# بحث على الويب
npm run cli task "ابحث عن Claude AI"

# تحليل Fantasy Football
npm run cli task "قارن بين Tyreek Hill و Davante Adams"

# عرض حالة الوكيل
npm run cli status
```

---

## 🎯 نصائح للاستخدام على Replit

### ✅ افضل الممارسات:

1. **Always On**:
   - في Replit المجاني، المشروع يتوقف بعد فترة
   - للإبقاء عليه شغال، استخدم Replit Hacker Plan (مدفوع)

2. **حفظ التغييرات**:
   - Replit يحفظ تلقائياً
   - لكن أفضل تعمل commit على Git من وقت للتاني

3. **Console Output**:
   - شوف نتائج الأوامر في تاب **Console**
   - إذا في خطأ، الرسالة تظهر بالأحمر

4. **Mobile Usage**:
   - على الموبايل، استخدم وضع Landscape (أفقي) للشاشة الأكبر
   - الـ Shell قد يكون صغير - استخدم zoom

---

## 🐛 حل المشاكل الشائعة

### المشكلة: `ANTHROPIC_API_KEY is required`

**السبب:** المفتاح مش موجود أو غلط

**الحل:**
1. تأكد أنك أضفت المفتاح في **Secrets** أو `.env`
2. المفتاح لازم يبدأ بـ `sk-ant-`
3. جرب تعمل Restart للـ Repl:
   - اضغط Stop (■) ثم Run (▶) مرة تانية

### المشكلة: `Cannot find module`

**السبب:** المكتبات مش مثبتة صح

**الحل:**
```bash
rm -rf node_modules package-lock.json
npm install
```

### المشكلة: `Permission denied`

**السبب:** ملف Shell Script مش Executable

**الحل:**
```bash
chmod +x start-agent.sh
./start-agent.sh
```

### المشكلة: الوكيل بطيء في الرد

**السبب:** Replit المجاني عنده حدود على المعالج

**الحل:**
- استخدم نموذج أصغر في `.env`:
  ```
  AGENT_MODEL=claude-3-haiku-20240307
  ```
- أو ارفع لـ Replit Hacker Plan

### المشكلة: `Error: timeout of 120000ms exceeded`

**السبب:** Replit عنده مشكلة في الاتصال بـ Anthropic API

**الحل:**
- انتظر دقيقة وحاول مرة تانية
- تأكد من اتصال النت
- تأكد أن API Key صحيح

---

## 🎨 تخصيص الوكيل

### تغيير اسم الوكيل:

عدل `.env`:
```bash
AGENT_NAME=مساعدي الشخصي
```

### تغيير النموذج المستخدم:

```bash
# للاستجابات السريعة والرخيصة
AGENT_MODEL=claude-3-haiku-20240307

# للذكاء المتوازن (افتراضي)
AGENT_MODEL=claude-sonnet-4-5

# للمهام المعقدة جداً
AGENT_MODEL=claude-opus-4
```

### تفعيل Scheduler:

```bash
ENABLE_SCHEDULER=true
SCHEDULER_TIMEZONE=Africa/Cairo
```

---

## 📊 مراقبة الاستخدام

### عرض إحصائيات الوكيل:

```bash
npm run cli status
```

سيعرض:
- عدد المحادثات
- المهام المحفوظة
- الذاكرة المستخدمة
- المهام المجدولة

### مسح السجلات:

```bash
npm run cli clear
```

---

## 💰 التكاليف المتوقعة

### Replit:
- **المجاني**: كافي للتجربة والاستخدام الخفيف
- **Hacker Plan ($7/شهر)**: للاستخدام الجدي

### Anthropic API:
- **المجاني**: $5 رصيد تجريبي عند التسجيل
- **بعد استنفاد الرصيد**:
  - Claude Haiku: ~$0.0001 لكل استعلام
  - Claude Sonnet: ~$0.001 لكل استعلام
  - Claude Opus: ~$0.01 لكل استعلام

**مثال على الاستخدام:**
- 100 محادثة يومياً بـ Sonnet = ~$3 شهرياً

---

## 🔗 روابط مهمة

- 🌐 Replit: https://replit.com
- 🤖 Anthropic Console: https://console.anthropic.com
- 📖 دليل Replit (عربي): https://docs.replit.com
- 🔑 Anthropic Pricing: https://www.anthropic.com/pricing

---

## ⚡ اختصارات مفيدة في Replit

- **Ctrl + S**: حفظ الملف
- **Ctrl + F**: بحث في الملف
- **Ctrl + `**: فتح/إغلاق Shell
- **Ctrl + Shift + F**: بحث في كل الملفات
- **F5**: إعادة تشغيل الـ Repl

---

## 🎓 تعلم أكثر

بعد ما تجرب الوكيل، اقرأ:
- 📖 `AGENT_GUIDE.md` - الدليل الشامل
- 🔧 `examples/` - أمثلة على المهام
- 📚 `docs/` - وثائق متقدمة

---

**صنع بـ ❤️ للمستخدمين العرب**

**جاهز؟ ابدأ الآن! 🚀**
