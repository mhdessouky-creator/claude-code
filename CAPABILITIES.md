# 🎯 إمكانيات الوكيل الذكي الحالية

## 📋 نظرة عامة

هذا الوكيل الذكي تم بناؤه باستخدام **Claude API** من Anthropic ويحتوي على **4 وحدات رئيسية** + نظام ذاكرة + جدولة مهام + دعم MCP.

---

## 🧠 النظام الأساسي (Core System)

### 1. الوكيل الرئيسي (AIAgent)
**الملف:** `src/core/agent.js`

#### الإمكانيات:
- ✅ محادثة تفاعلية مع Claude
- ✅ ذاكرة سياقية (يتذكر محادثاتك السابقة)
- ✅ معالجة رسائل بالعربي والإنجليزي
- ✅ تنفيذ مهام تلقائياً من المحادثة
- ✅ دعم Model Context Protocol (MCP)
- ✅ إدارة تكوينات متعددة

#### الاستخدام:
```javascript
const agent = new AIAgent({
  name: 'مساعدي الشخصي',
  model: 'claude-sonnet-4-5'
});

await agent.initialize();
const response = await agent.processMessage('مرحباً!');
```

---

### 2. إدارة الذاكرة (MemoryManager)
**الملف:** `src/core/memory.js`

#### الإمكانيات:
- ✅ حفظ المحادثات في قاعدة بيانات SQLite
- ✅ البحث في المحادثات السابقة
- ✅ حفظ تفضيلات المستخدم
- ✅ حفظ المهام المنفذة
- ✅ مسح البيانات القديمة تلقائياً
- ✅ إحصائيات الاستخدام

#### الاستخدام:
```bash
# عرض الإحصائيات
npm run cli status

# مسح السجلات القديمة
npm run cli clear
```

---

### 3. منفذ المهام (TaskExecutor)
**الملف:** `src/core/executor.js`

#### الإمكانيات:
- ✅ توجيه المهام للوحدة المناسبة
- ✅ جدولة مهام متكررة (Cron Jobs)
- ✅ تتبع المهام الجارية
- ✅ إلغاء المهام المجدولة
- ✅ تحليل نوع المهمة تلقائياً

#### الاستخدام:
```bash
# جدولة مهمة يومية
npm run cli schedule "تنظيف الملفات" "0 9 * * *"
```

---

### 4. دعم MCP (Model Context Protocol)
**الملف:** `src/core/mcp.js`

#### الإمكانيات:
- ✅ إضافة خوادم MCP خارجية
- ✅ اكتشاف الأدوات المتاحة
- ✅ استدعاء أدوات MCP من المحادثة
- ✅ إدارة عدة خوادم MCP

#### الخوادم الجاهزة:
- 📧 Google Workspace (Gmail, Calendar, Drive)
- 📝 Notion
- 📊 Airtable
- 💬 Telegram Bot
- 📱 WhatsApp Business
- 🐦 Twitter/X API
- 🔴 Reddit
- 📁 Filesystem Operations

#### الاستخدام:
```bash
# إضافة خادم MCP
npm run cli mcp:add notion http://localhost:3002

# عرض الخوادم
npm run cli mcp:list

# اكتشاف الأدوات
npm run cli mcp:discover
```

---

## 📦 الوحدات المتاحة (Task Modules)

### 1️⃣ وحدة الملفات (FileModule)
**الملف:** `src/modules/files.js`

#### الإمكانيات:

##### تنظيم الملفات (Organize):
- ✅ تنظيم حسب النوع (PDF, JPG, MP3, إلخ)
- ✅ إنشاء مجلدات تلقائياً
- ✅ نقل الملفات للمجلد المناسب

**مثال:**
```bash
npm run cli task "نظم ملفات Downloads حسب النوع"
```

**النتيجة:**
```
Downloads/
├── PDF/
│   ├── document1.pdf
│   └── document2.pdf
├── JPG/
│   ├── photo1.jpg
│   └── photo2.jpg
└── MP3/
    └── song.mp3
```

##### البحث عن الملفات (Search):
- ✅ بحث متكرر في المجلدات الفرعية
- ✅ بحث بالاسم
- ✅ تجاهل المجلدات غير المسموحة

**مثال:**
```bash
npm run cli task "ابحث عن ملفات PDF في Documents"
```

##### النسخ الاحتياطي (Backup):
- ✅ نسخ المجلد كاملاً
- ✅ إضافة timestamp للنسخة
- ✅ حفظ في مجلد backups

**مثال:**
```bash
npm run cli task "اعمل نسخة احتياطية من مجلد Documents"
```

**النتيجة:**
```
backups/Documents_1234567890/
```

##### تنظيف الملفات (Clean):
- ✅ حذف الملفات الأقدم من 30 يوم
- ✅ تقرير بعدد الملفات المحذوفة
- ✅ آمن (لا يحذف مجلدات)

**مثال:**
```bash
npm run cli task "احذف الملفات القديمة في مجلد temp"
```

---

### 2️⃣ وحدة الويب (WebModule)
**الملف:** `src/modules/web.js`

#### الإمكانيات:

##### البحث على الويب (Search):
- ✅ استخراج استعلام البحث
- ✅ جاهز للتكامل مع Google/DuckDuckGo API
- ⚠️ حالياً: يعيد placeholder (يحتاج تفعيل API)

**مثال:**
```bash
npm run cli task "ابحث عن أحدث أخبار الذكاء الاصطناعي"
```

##### جلب محتوى URL (Fetch):
- ✅ تحميل صفحات الويب
- ✅ عرض معلومات الصفحة (حجم، نوع المحتوى)
- ✅ معاينة المحتوى

**مثال:**
```bash
npm run cli task "اجلب محتوى https://example.com"
```

##### مراقبة المواقع (Monitor):
- ✅ حفظ حالة الموقع الحالية
- ✅ كشف التغييرات باستخدام Hash
- ✅ حفظ تاريخ آخر فحص

**مثال:**
```bash
npm run cli task "راقب التغييرات في https://example.com"
```

---

### 3️⃣ وحدة البريد الإلكتروني (EmailModule)
**الملف:** `src/modules/email.js`

#### الإمكانيات:

##### إرسال بريد (Send):
- ✅ إرسال عبر SMTP
- ✅ دعم Gmail/Outlook
- ✅ استخراج تفاصيل البريد من الطلب بالذكاء الاصطناعي
- ✅ دعم HTML/Text

**الإعداد المطلوب في `.env`:**
```bash
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
```

**مثال:**
```bash
npm run cli task "ارسل إيميل لـ user@example.com بعنوان 'تذكير' ومحتوى 'لا تنسى الاجتماع'"
```

##### جدولة بريد:
- ⚠️ قيد التطوير

---

### 4️⃣ وحدة Fantasy Football
**الملف:** `src/modules/fantasy-football.js`

#### الإمكانيات:

##### مقارنة اللاعبين (Compare):
- ✅ مقارنة بين 2+ لاعبين
- ✅ تحليل الأداء الحالي
- ✅ توصية بمن تبدأ (Start/Sit)

**مثال:**
```bash
npm run cli task "قارن بين Tyreek Hill و Davante Adams"
```

##### تحسين التشكيلة (Optimize):
- ✅ اقتراح أفضل تشكيلة
- ✅ مراعاة Bye Weeks
- ✅ تحليل المباريات

**مثال:**
```bash
npm run cli task "احسن تشكيلتي للأسبوع القادم"
```

##### تحليل الصفقات (Trade Analysis):
- ✅ تقييم عروض التبادل
- ✅ توصية قبول/رفض
- ✅ تحليل القيمة

**مثال:**
```bash
npm run cli task "هل أقبل صفقة Kelce مقابل Andrews و Kupp؟"
```

##### تحليل المباراة (Matchup):
- ✅ تحليل مباراة اللاعب ضد الدفاع
- ✅ إحصائيات الدفاع المنافس
- ✅ توقعات الأداء

**مثال:**
```bash
npm run cli task "كيف أداء Mahomes ضد دفاع Buffalo؟"
```

##### تأثير الإصابات (Injury Impact):
- ✅ تحليل تأثير إصابة لاعب
- ✅ اقتراح بدائل
- ✅ تأثير على قيمة اللاعبين الآخرين

**مثال:**
```bash
npm run cli task "تأثير إصابة CMC على Deebo Samuel"
```

##### محاكاة السيناريوهات (Scenario):
- ✅ محاكاة "ماذا لو"
- ✅ توقعات الفوز/الخسارة

**مثال:**
```bash
npm run cli task "ماذا لو بدأت Hurts بدلاً من Allen؟"
```

##### توصيات Waiver Wire:
- ✅ اقتراح لاعبين للإضافة
- ✅ لاعبين للتخلي عنهم
- ✅ تحليل الأولويات

**مثال:**
```bash
npm run cli task "من أضيف من Waiver Wire هذا الأسبوع؟"
```

##### ترتيب باقي الموسم (ROS Rankings):
- ✅ توقعات لباقي الموسم
- ✅ مراعاة الجدول والإصابات

**مثال:**
```bash
npm run cli task "رتب Running Backs لباقي الموسم"
```

---

## 🎮 واجهة CLI التفاعلية

### الأوامر المتاحة:

#### 1. محادثة تفاعلية:
```bash
npm run cli chat
```

**المميزات:**
- ✅ محادثة مستمرة
- ✅ حفظ السياق
- ✅ تنفيذ مهام من المحادثة
- ✅ الخروج بكتابة `/exit`

---

#### 2. تنفيذ مهمة واحدة:
```bash
npm run cli task "وصف المهمة"
```

**المميزات:**
- ✅ تنفيذ سريع
- ✅ إغلاق تلقائي بعد الانتهاء

---

#### 3. جدولة مهام:
```bash
npm run cli schedule "المهمة" "صيغة Cron"
```

**أمثلة على صيغة Cron:**
```bash
# كل يوم الساعة 9 صباحاً
"0 9 * * *"

# كل ساعة
"0 * * * *"

# كل يوم اثنين الساعة 2 ظهراً
"0 14 * * 1"

# كل 30 دقيقة
"*/30 * * * *"
```

---

#### 4. عرض الحالة:
```bash
npm run cli status
```

**يعرض:**
- 📊 اسم الوكيل
- 🤖 النموذج المستخدم
- 📈 عدد المحادثات
- 💾 إحصائيات الذاكرة
- ⏰ المهام المجدولة
- 🔌 خوادم MCP

---

#### 5. مسح السجلات:
```bash
npm run cli clear
```

---

#### 6. إدارة MCP:

```bash
# إضافة خادم
npm run cli mcp:add <name> <url> [-t <token>]

# حذف خادم
npm run cli mcp:remove <name>

# عرض الخوادم
npm run cli mcp:list

# اكتشاف الأدوات
npm run cli mcp:discover
```

---

## 🔧 الإعدادات المتاحة (Environment Variables)

### الإعدادات الأساسية:

```bash
# Anthropic API
ANTHROPIC_API_KEY=sk-ant-...       # مطلوب
AGENT_MODEL=claude-sonnet-4-5      # النموذج
AGENT_NAME=Digital Assistant       # اسم الوكيل

# الذاكرة
DB_PATH=./data/agent-memory.db     # قاعدة البيانات

# MCP
ENABLE_MCP=true                    # تفعيل/تعطيل MCP

# الجدولة
ENABLE_SCHEDULER=false             # تفعيل المهام المجدولة
SCHEDULER_TIMEZONE=Africa/Cairo    # المنطقة الزمنية

# البريد الإلكتروني (اختياري)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# السجلات
LOG_LEVEL=info                     # debug/info/warn/error
LOG_FILE=./logs/agent.log
```

---

## 📊 النماذج المتاحة (Claude Models)

### 1. Claude 3 Haiku (الأسرع والأرخص):
```bash
AGENT_MODEL=claude-3-haiku-20240307
```
- ⚡ سريع جداً
- 💰 رخيص (~$0.0001/استعلام)
- ✅ مثالي للمهام البسيطة

### 2. Claude Sonnet 4.5 (متوازن - افتراضي):
```bash
AGENT_MODEL=claude-sonnet-4-5
```
- ⚖️ متوازن (سرعة + ذكاء)
- 💰 معتدل (~$0.001/استعلام)
- ✅ مثالي لمعظم المهام

### 3. Claude Opus 4 (الأذكى):
```bash
AGENT_MODEL=claude-opus-4
```
- 🧠 الأذكى والأدق
- 💰 غالي (~$0.01/استعلام)
- ✅ للمهام المعقدة جداً

---

## 💾 البيانات المحفوظة

### قاعدة البيانات SQLite:
**المسار:** `./data/agent-memory.db`

**الجداول:**
- `conversations` - المحادثات
- `tasks` - المهام المنفذة
- `preferences` - تفضيلات المستخدم
- `scheduled_tasks` - المهام المجدولة

---

## 🚧 ما في التطوير (Roadmap)

### قريباً:
- [ ] واجهة ويب (Web UI)
- [ ] دعم الأوامر الصوتية
- [ ] تكامل Google Calendar الكامل
- [ ] نظام Plugins للإضافات
- [ ] Search API Integration (Google/Bing)

### مستقبلاً:
- [ ] تحسين الذاكرة باستخدام Embeddings
- [ ] Multi-agent system
- [ ] دعم الصور والملفات
- [ ] تطبيق موبايل

---

## 🎯 أمثلة عملية كاملة

### مثال 1: إدارة الملفات اليومية
```bash
# 1. تنظيم Downloads
npm run cli task "نظم ملفات Downloads حسب النوع"

# 2. نسخ احتياطي للمستندات
npm run cli task "اعمل backup لمجلد Documents"

# 3. حذف الملفات القديمة
npm run cli task "احذف الملفات الأقدم من 30 يوم في temp"

# 4. جدولة تنظيف يومي (9 صباحاً)
npm run cli schedule "تنظيف الملفات المؤقتة" "0 9 * * *"
```

### مثال 2: Fantasy Football - يوم المباريات
```bash
# 1. تحسين التشكيلة
npm run cli task "احسن تشكيلتي للأسبوع 12"

# 2. قرار Start/Sit
npm run cli task "هل أبدأ Kamara أو Jacobs؟"

# 3. تحليل صفقة
npm run cli task "هل أقبل Jefferson مقابل Chase و Pollard؟"

# 4. Waiver Wire
npm run cli task "من أضيف من Waiver هذا الأسبوع؟"
```

### مثال 3: إنتاجية يومية
```bash
# محادثة تفاعلية
npm run cli chat

You: صباح الخير! ما المهام اليوم؟
Agent: صباح الخير! دعني أساعدك...

You: نظم ملفات Downloads
Agent: ✅ تم تنظيم 25 ملف

You: ذكرني بالاجتماع في الساعة 3
Agent: ✅ تم ضبط التذكير

You: /exit
```

---

## 📈 الإحصائيات والأداء

### استخدام الـ Tokens:
- محادثة بسيطة: ~500-1000 token
- مهمة متوسطة: ~1000-2000 token
- تحليل معقد: ~2000-4000 token

### سرعة الاستجابة:
- Haiku: 1-3 ثانية
- Sonnet: 2-5 ثواني
- Opus: 3-7 ثواني

---

## 🔐 الأمان والخصوصية

### ما يُحفظ محلياً:
- ✅ المحادثات
- ✅ المهام المنفذة
- ✅ التفضيلات

### ما يُرسل لـ Anthropic:
- ✅ نص المحادثة فقط
- ❌ لا ملفات
- ❌ لا بيانات حساسة (إلا إذا كتبتها أنت)

### حماية API Key:
- ✅ محفوظ في `.env` (مُستثنى من Git)
- ✅ في Replit يُحفظ في Secrets
- ⚠️ لا تشاركه أبداً

---

**الوكيل جاهز بكل إمكانياته! 🚀**
