# 🤖 Digital Life AI Agent - دليل المستخدم الشامل

## 📋 نظرة عامة

Digital Life AI Agent هو وكيل ذكاء اصطناعي مبني باستخدام Claude API لأتمتة المهام الرقمية اليومية. يمكنه:

- 📧 إدارة البريد الإلكتروني
- 📁 تنظيم الملفات
- 🌐 البحث على الويب وجمع المعلومات
- 💾 تذكر السياق والتفضيلات
- ⏰ جدولة المهام المتكررة
- 🔄 التعلم من التفاعلات السابقة

## 🚀 البدء السريع

### المتطلبات

- Node.js 18+
- Anthropic API Key
- نظام Linux/macOS/Windows

### التثبيت

```bash
# 1. استنساخ المشروع
git clone https://github.com/mhdessouky-creator/claude-code.git
cd claude-code

# 2. تثبيت المكتبات
npm install

# 3. إعداد البيئة
cp .env.example .env
# عدّل .env وأضف API key الخاص بك

# 4. بناء المشروع
npm run build

# 5. تشغيل الوكيل
npm start
```

### الحصول على API Key

1. سجل في [Anthropic Console](https://console.anthropic.com)
2. اذهب إلى API Keys
3. أنشئ مفتاح جديد
4. أضفه في `.env`:
   ```
   ANTHROPIC_API_KEY=sk-ant-...
   ```

## 💡 أوضاع الاستخدام

### 1. الوضع التفاعلي (Chat Mode)

أفضل وضع للمحادثات الطويلة والمهام المعقدة:

```bash
npm run cli chat
```

مثال على محادثة:

```
You: مرحباً! ما الذي يمكنك مساعدتي به؟
🤖 Agent: مرحباً! يمكنني مساعدتك في:
- إدارة البريد الإلكتروني
- تنظيم الملفات
- البحث عن معلومات
- جدولة المهام
... إلخ

You: نظم الملفات في مجلد Downloads
🤖 Agent: سأقوم بتنظيم ملفات Downloads حسب النوع...
📋 Task executed: organize files in Downloads
```

### 2. تنفيذ مهمة مباشرة

لتنفيذ مهمة واحدة وإنهاء البرنامج:

```bash
npm run cli task "ابحث عن أحدث أخبار الذكاء الاصطناعي"
```

### 3. جدولة مهام متكررة

لجدولة مهمة تتكرر تلقائياً:

```bash
# تشغيل كل يوم الساعة 9 صباحاً
npm run cli schedule "تنظيف الملفات المؤقتة" "0 9 * * *"

# تشغيل كل ساعة
npm run cli schedule "التحقق من البريد الوارد" "0 * * * *"
```

صيغة Cron:
```
* * * * *
│ │ │ │ │
│ │ │ │ └─ يوم الأسبوع (0-7)
│ │ │ └─── شهر (1-12)
│ │ └───── يوم الشهر (1-31)
│ └─────── ساعة (0-23)
└───────── دقيقة (0-59)
```

### 4. عرض الحالة

```bash
npm run cli status
```

يعرض:
- اسم الوكيل ونموذج Claude المستخدم
- عدد المحادثات
- إحصائيات الذاكرة
- المهام المجدولة

## 📚 أمثلة عملية

### إدارة البريد الإلكتروني

```javascript
// عبر CLI
"أرسل بريد إلكتروني إلى ahmed@example.com بعنوان 'اجتماع اليوم' والمحتوى 'نلتقي الساعة 3 مساءً'"

// أو برمجياً
import { AIAgent } from './src/index.js';

const agent = new AIAgent();
await agent.initialize();

await agent.processMessage(
  "أرسل تقرير المبيعات إلى المدير"
);
```

### تنظيم الملفات

```javascript
"نظم جميع الملفات في مجلد Downloads حسب النوع"
"ابحث عن ملفات PDF في المجلد Documents"
"اعمل نسخة احتياطية من مجلد Projects"
"احذف الملفات المؤقتة الأقدم من شهر"
```

### البحث والمعلومات

```javascript
"ابحث عن معلومات عن GPT-4"
"احصل على محتوى الصفحة https://example.com"
"راقب موقع example.com وأخبرني إذا تغير"
```

### المهام المجدولة

```javascript
// كل يوم الساعة 8 صباحاً
await agent.scheduleTask(
  "تنظيف سلة المحذوفات",
  "0 8 * * *"
);

// كل ساعتين
await agent.scheduleTask(
  "التحقق من التحديثات",
  "0 */2 * * *"
);
```

## 🏗️ البنية المعمارية

```
digital-life-ai-agent/
├── src/
│   ├── core/           # النظام الأساسي
│   │   ├── agent.js    # الوكيل الرئيسي
│   │   ├── memory.js   # إدارة الذاكرة
│   │   └── executor.js # تنفيذ المهام
│   ├── modules/        # وحدات المهام
│   │   ├── email.js    # وحدة البريد
│   │   ├── files.js    # وحدة الملفات
│   │   └── web.js      # وحدة الويب
│   ├── utils/          # أدوات مساعدة
│   │   └── logger.js   # نظام السجلات
│   ├── cli.js          # واجهة CLI
│   └── index.js        # نقطة البداية
├── tests/              # الاختبارات
├── data/               # قاعدة البيانات
├── logs/               # ملفات السجلات
└── backups/            # النسخ الاحتياطية
```

## 🔧 التخصيص والإعدادات

### ملف .env

```bash
# إعدادات Claude API
ANTHROPIC_API_KEY=sk-ant-...
AGENT_NAME=MyAssistant
AGENT_MODEL=claude-3-5-sonnet-20241022

# قاعدة البيانات
DB_PATH=./data/agent-memory.db

# البريد الإلكتروني
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your@email.com
EMAIL_PASS=your_app_password

# الجدولة
ENABLE_SCHEDULER=true
SCHEDULER_TIMEZONE=Africa/Cairo

# السجلات
LOG_LEVEL=info  # debug, info, warn, error
LOG_FILE=./logs/agent.log
```

### إضافة وحدات مخصصة

يمكنك إضافة وحدات جديدة بسهولة:

```javascript
// src/modules/calendar.js
export class CalendarModule {
  constructor(agent) {
    this.agent = agent;
  }

  async execute(task) {
    // منطق الوحدة
    return {
      success: true,
      message: 'Task completed'
    };
  }
}
```

## 📊 نظام الذاكرة

الوكيل يحتفظ بذاكرة دائمة تشمل:

- **المحادثات السابقة**: للحفاظ على السياق
- **تفضيلات المستخدم**: لتخصيص الاستجابات
- **سجل المهام**: لتتبع ما تم إنجازه

```javascript
// الوصول للذاكرة برمجياً
const memories = await agent.memory.searchMemories("email", 5);
const preference = await agent.memory.getPreference("language");
await agent.memory.savePreference("language", "ar");
```

## 🔒 الأمان والخصوصية

- ✅ جميع البيانات مخزنة محلياً
- ✅ لا تُرسل معلومات حساسة إلا لـ Claude API
- ✅ يمكن حذف الذاكرة في أي وقت
- ⚠️ لا تشارك API key مع أحد
- ⚠️ استخدم App Passwords للبريد الإلكتروني

## 🐛 حل المشاكل

### المشكلة: API Key غير صحيح

```bash
Error: ANTHROPIC_API_KEY is required
```

**الحل**: تأكد من إضافة المفتاح في `.env`

### المشكلة: فشل إرسال البريد

```bash
Email not configured
```

**الحل**: أضف إعدادات البريد في `.env`

### المشكلة: لا توجد نتائج بحث

**الحل**: الوحدة تحتاج API للبحث - راجع src/modules/web.js

## 🛣️ خارطة الطريق

- [ ] إضافة واجهة ويب
- [ ] دعم المزيد من اللغات
- [ ] تكامل مع تقويم Google
- [ ] دعم الأوامر الصوتية
- [ ] نظام Plugins
- [ ] تحسين الذاكرة باستخدام Embeddings

## 🤝 المساهمة

نرحب بمساهماتك!

1. Fork المشروع
2. أنشئ فرع للميزة الجديدة
3. Commit التغييرات
4. Push إلى الفرع
5. افتح Pull Request

## 📝 الترخيص

MIT License - استخدم بحرية!

## 📧 الدعم

- افتح Issue في GitHub
- راسلني عبر البريد
- تابع التحديثات على المشروع

---

**صُنع بـ ❤️ باستخدام Claude AI**
