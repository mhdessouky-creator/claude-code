# Branch: feature/agent-setup 🚀

## الغرض من هذا الـ Branch

هذا الـ branch يحتوي على **تعليمات وإعدادات الوكيل الذكي** فقط.

## المحتويات

### ملفات الإعداد الأساسية
- `AGENT_GUIDE.md` - دليل شامل لاستخدام الوكيل
- `TERMUX_GUIDE.md` - دليل تشغيل الوكيل في Termux
- `.env.example` - مثال لملف البيئة مع جميع الإعدادات
- `.env.agent` - إعدادات الوكيل الأساسية
- `start-agent.sh` - سكريبت بدء الوكيل في Termux

### ملفات التكوين
- `config/settings.py` - إعدادات Python Agent
- `requirements.txt` - متطلبات Python
- `package.json` - متطلبات Node.js

### الكود المرجعي (للفهم فقط)
- `main.py` - نقطة الدخول الرئيسية للوكيل Python
- `agents/` - وكلاء Python الأساسيين

## كيفية الاستخدام

### 1. إعداد البيئة
```bash
# استنساخ المستودع
git clone <repo-url>
cd claude-code

# التبديل لهذا الـ branch
git checkout feature/agent-setup

# نسخ ملف البيئة
cp .env.example .env

# تعديل المفاتيح
nano .env
```

### 2. تثبيت المتطلبات

**Python:**
```bash
pip install -r requirements.txt
```

**Node.js:**
```bash
npm install
```

### 3. تشغيل الوكيل

**في Termux:**
```bash
./start-agent.sh
```

**مباشرة:**
```bash
python main.py
```

## الـ Branches الأخرى

- `feature/documentation` - التوثيق الكامل
- `feature/mcp` - MCP servers
- `feature/tools` - الأدوات المساعدة
- `feature/integrations` - التكاملات (Gmail, etc.)
- `feature/skills` - المهارات والأمثلة

## المساعدة

للمزيد من المعلومات، راجع:
- [AGENT_GUIDE.md](./AGENT_GUIDE.md) - دليل شامل
- [TERMUX_GUIDE.md](./TERMUX_GUIDE.md) - دليل Termux

---

**ملاحظة:** هذا الـ branch يحتوي فقط على ملفات الإعداد والتكوين. للحصول على الوظائف الكاملة، راجع الـ branches الأخرى.
